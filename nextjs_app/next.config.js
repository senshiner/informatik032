/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // Keep default public folder behavior; static assets will be copied to /public
};
module.exports = nextConfig;
